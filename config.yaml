markets:
  ieso:
    xml_dir: "data/ieso/xml/"
    default_city: "Goderich"
    timezone: "UTC"

  aeso:
    csv_dir: "data/aeso_rawcsv"
    default_city: "Red Deer"
    timezone: "America/Edmonton"

    # ✅ NEW: use precomputed master (fast, no timeout)
    precomputed_master: "data/aeso_precomputed/aeso_master.csv"